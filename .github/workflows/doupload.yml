name: Mod Portal Upload

on: workflow_dispatch

permissions:
  contents: write

jobs:
  portal-upload:
    name: "Upload to mod portal"
    uses: pyanodon/pyanodontests/.github/workflows/portal-upload.yml@main
    secrets: inherit


